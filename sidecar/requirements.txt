# Sidecar 运行时依赖（Python 3.11+）
# 沿用旧 PyQt 版的核心音频栈
numpy>=1.26
soundfile>=0.12

# 新增：sidecar HTTP / agent 层
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# 文件操作（移到回收站，跨平台）
send2trash>=1.8

# 测试
pytest>=8
httpx>=0.27
